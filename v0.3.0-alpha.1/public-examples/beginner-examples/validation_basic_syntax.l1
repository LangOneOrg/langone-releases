// Basic Syntax Validation Test
println("=== Basic Syntax Tests ===");

// Variables and assignments
let integer_var = 42;
let float_var = 3.14159;
let string_var = "Hello, LangOne!";
let boolean_var = true;

println("Integer: " + to_string(integer_var));
println("Float: " + to_string(float_var));
println("String: " + string_var);
println("Boolean: " + to_string(boolean_var));

// Arithmetic operations
let a = 10;
let b = 3;
println("Addition: " + to_string(a + b));
println("Subtraction: " + to_string(a - b));
println("Multiplication: " + to_string(a * b));
println("Division: " + to_string(a / b));
println("Modulo: " + to_string(a % b));

// Mixed type operations (should work with type coercion)
let mixed_sum = a + float_var;
println("Mixed types (int + float): " + to_string(mixed_sum));

println("✅ Basic syntax validation complete");
