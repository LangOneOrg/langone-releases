// Simple LangOne Demo
print("Hello from LangOne!");
print("===================");

// Variables
let name = "LangOne";
let version = "0.1.0-alpha.1";

print("Language: " + name);
print("Version: " + version);

// Math
let a = 15;
let b = 3;
print("Math: " + to_string(a) + " + " + to_string(b) + " = " + to_string(a + b));
print("Math: " + to_string(a) + " * " + to_string(b) + " = " + to_string(a * b));

// Function call
print("Length of name: " + to_string(len(name)));
