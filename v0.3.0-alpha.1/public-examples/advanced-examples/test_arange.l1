// Test arange function
println("🚀 Testing arange function");

let range1 = arange(0, 10, 2);
println("Range 0-10 step 2: " + to_string(range1));

let range2 = arange(1, 5, 1);
println("Range 1-5 step 1: " + to_string(range2));

println("✅ arange test complete!");
