// Minimal Array Test
println("🚀 LangOne Array/Tensor Core - Minimal Test");
println("==========================================");

// Test 1: Basic array creation
let arr = array([10, 20, 30, 40, 50]);
println("1. Array creation: " + to_string(arr));

// Test 2: Zeros
let zeros = zeros(5);
println("2. Zeros: " + to_string(zeros));

// Test 3: Ones
let ones = ones(5);
println("3. Ones: " + to_string(ones));

// Test 4: Arange
let range = arange(0, 10, 2);
println("4. Arange: " + to_string(range));

// Test 5: Linspace
let linspace = linspace(0, 1, 5);
println("5. Linspace: " + to_string(linspace));

println("✅ All tests passed!");
